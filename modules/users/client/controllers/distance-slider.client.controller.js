'use strict';

angular.module('users').controller('DistanceSliderController', ['$scope',
  function ($scope) {
    // Distance slider controller logic
    // ...
    $scope.range = $scope.parentRange;
  }
]);
